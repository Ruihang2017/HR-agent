# Copy this file to .env (in backend/) and fill in your key. .env is gitignored.
# The backend reads OPENAI_API_KEY from backend/.env automatically — no shell export needed.
OPENAI_API_KEY=sk-your-openai-key-here
